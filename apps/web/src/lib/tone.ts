// Shared status-color vocabulary for the account-area list cards
// (bookings, subscriptions, notifications) — see .list-card/.status-pill
// tone-* rules in styles/index.css.
export type Tone = "neutral" | "warning" | "primary" | "accent" | "success" | "error";
